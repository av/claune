name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.1'
          cache: true

      - name: Install ALSA (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y alsa-utils

      - name: Build
        run: go build -o claune${{ runner.os == 'Windows' && '.exe' || '' }} ./cmd/claune

      - name: Run E2E
        shell: bash
        run: |
          export PATH="$PWD:$PATH"
          export CLAUNE_CMD="claune"
          ./scripts/e2e-test.sh
